name = "etihad-lead-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

[vars]
ALLOWED_ORIGIN = "https://hizmettouruz-dev.github.io"

# Secrets — set these, do NOT put them here:
#   wrangler secret put BOT_TOKEN
#   wrangler secret put TARGET_CHAT_ID
